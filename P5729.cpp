#include <iostream>
#include <string.h>
using namespace std;
int A[20][20][20], w, x, h;
int cut()
int main()
{
    int q;
    cin >> w >> x >> h >> q;
    memset(A, 0, sizeof(A));
    memset(A, 1, A[x][y][q]);

    return 0;
}